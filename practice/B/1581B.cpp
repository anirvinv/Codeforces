#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define fo(i, n) for (int i = 0; i < n; i++)
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        int n, m, k;
        cin >> n >> m >> k;
        int maxDia = 0;
        if (m < n)
        {
            maxDia = m;
        }
        else if (m == n)
        {
            maxDia = m / 2;
        }
        else
        {
            // bruv idk wtf this problem makes no sense
        }
    }
}